# 01 — Reconnaissance

## Network Service Discovery

The reconnaissance phase was performed from the attacker machine (`192.168.10.133`) against the Windows endpoint (`192.168.10.132`) and Domain Controller (`192.168.10.130`).

### Nmap Scan

Nmap was used with `-Pn` to perform TCP port scanning:

```bash
nmap -Pn 192.168.10.132
nmap -Pn 192.168.10.130
```

The scans identified exposed Windows and Active Directory services.

![Nmap Port Scan](../images/reconnaissance-port-scan/attacker-port-scan.png)

### Microsoft Defender for Endpoint

Microsoft Defender detected a TCP port scan on the victim machine originating from `192.168.10.133`, with **1000 ports scanned**.

The activity was mapped to **T1046 — Network Service Discovery**.

![EDR Port Scan - Victim](../images/reconnaissance-port-scan/edr-port-scan-victim.png)

The Domain Controller also generated an **Inbound Vertical Port Scan** detection from `192.168.10.133`.

![EDR Port Scan - DC](../images/reconnaissance-port-scan/edr-port-scan-dc.png)

### Microsoft Sentinel

Windows Security Events `5156` and `5152` were queried in Microsoft Sentinel to validate the network connections generated by the scan.

![Sentinel Port Scan - Victim](../images/reconnaissance-port-scan/sentinel-port-scan-victim.png)

The same investigation was performed against the Domain Controller.

![Sentinel Port Scan - DC](../images/reconnaissance-port-scan/sentinel-port-scan-dc.png)

### MITRE ATT&CK

* **T1046 — Network Service Discovery**

### Result

The reconnaissance activity was successfully observed across **Nmap, Microsoft Defender for Endpoint, and Microsoft Sentinel**.
